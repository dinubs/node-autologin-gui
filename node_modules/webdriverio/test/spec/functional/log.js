describe('logging', function() {

    before(h.setup());

    it('logType should return an array with log types', function() {
        return this.client.logTypes().then(function(res) {
            var types = res.value;

            expect(types).to.be.type(Array);

        });
    });

});